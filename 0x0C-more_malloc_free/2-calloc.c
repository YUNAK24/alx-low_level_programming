#include <stdlib.h>
#include "main.h"

/**
 * *_calloc - allocate memory for an array
 * @s: size of memory to be allocated
 * Return: pointer to the allocated memory
 */
void *_calloc(unsigned int nmmeb, unsigned int size)
{
	unsigned int i;

	for (i = 0; i < n; i++)
	{
		s[i] = b;
	}

	return (s);
}
